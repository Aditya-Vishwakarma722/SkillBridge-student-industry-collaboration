{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch SkillBridge Portal in Chrome",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}